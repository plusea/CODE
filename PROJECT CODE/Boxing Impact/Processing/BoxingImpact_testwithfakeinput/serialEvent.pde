/*
void serialEvent (Serial myPort) {
 String inString = myPort.readStringUntil('\n');  // get the ASCII string:
 if (inString != null) {  // if it's not empty:    
 inString = trim(inString);  // trim off any whitespace:
 int incomingValue= int(inString);  // convert to an array of ints:
 
 
 //transferValue = 1023 - incomingValue;
 //println("transfer values: " + transferValue);
 
 smoothing();
 //graph(transferValue);
 //graph();
 //peekDetection();
 }
 }
 
 */
