void displayAlphabet(int myNumber, int myTime){
  switch(myNumber){

    // A
  case 1:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<A_segments[0]+1;j++) {
        displaySegment(A_segments[j]);
      }
    }
    break;

    // B
  case 2:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<B_segments[0]+1;j++) {
        displaySegment(B_segments[j]);
      }
    }
    break;

    // C
  case 3:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<C_segments[0]+1;j++) {
        displaySegment(C_segments[j]);
      }
    }
    break;

    // D
  case 4:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<D_segments[0]+1;j++) {
        displaySegment(D_segments[j]);
      }
    }
    break;

    // E
  case 5:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<E_segments[0]+1;j++) {
        displaySegment(E_segments[j]);
      }
    }
    break;

    // F
  case 6:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<F_segments[0]+1;j++) {
        displaySegment(F_segments[j]);
      }
    }
    break;

    // G
  case 7:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<G_segments[0]+1;j++) {
        displaySegment(G_segments[j]);
      }
    }
    break;

    // H
  case 8:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<H_segments[0]+1;j++) {
        displaySegment(H_segments[j]);
      }
    }
    break;


    // I
  case 9:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<I_segments[0]+1;j++) {
        displaySegment(I_segments[j]);
      }
    }
    break;

    // J
  case 10:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<J_segments[0]+1;j++) {
        displaySegment(J_segments[j]);
      }
    }
    break;

    // K
  case 11:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<K_segments[0]+1;j++) {
        displaySegment(K_segments[j]);
      }
    }
    break;

    // L
  case 12:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<L_segments[0]+1;j++) {
        displaySegment(L_segments[j]);
      }
    }
    break;


    // M
  case 13:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<M_segments[0]+1;j++) {
        displaySegment(M_segments[j]);
      }
    }
    break;


    // N
  case 14:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<N_segments[0]+1;j++) {
        displaySegment(N_segments[j]);
      }
    }
    break;


    // O
  case 15:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<O_segments[0]+1;j++) {
        displaySegment(O_segments[j]);
      }
    }
    break;


    // P
  case 16:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<P_segments[0]+1;j++) {
        displaySegment(P_segments[j]);
      }
    }
    break;

    // Q
  case 17:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<Q_segments[0]+1;j++) {
        displaySegment(Q_segments[j]);
      }
    }
    break;


    // R
  case 18:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<R_segments[0]+1;j++) {
        displaySegment(R_segments[j]);
      }
    }
    break;

    // S
  case 19:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<S_segments[0]+1;j++) {
        displaySegment(S_segments[j]);
      }
    }
    break;

    // T
  case 20:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<T_segments[0]+1;j++) {
        displaySegment(T_segments[j]);
      }
    }
    break;

    // U
  case 21:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<U_segments[0]+1;j++) {
        displaySegment(U_segments[j]);
      }
    }
    break;

    // V
  case 22:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<V_segments[0]+1;j++) {
        displaySegment(V_segments[j]);
      }
    }
    break;

    // W
  case 23:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<W_segments[0]+1;j++) {
        displaySegment(W_segments[j]);
      }
    }
    break;


    // X
  case 24:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<X_segments[0]+1;j++) {
        displaySegment(X_segments[j]);
      }
    }
    break;


    // Y
  case 25:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<Y_segments[0]+1;j++) {
        displaySegment(Y_segments[j]);
      }
    }
    break;




    // Z
  case 26:
    for (int i=0;i<myTime;i++) {
      for (int j=1;j<Z_segments[0]+1;j++) {
        displaySegment(Z_segments[j]);
      }
    }
    break;

  }
}




